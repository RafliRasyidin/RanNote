<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rasyidin.rannote">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RanNote">
        <activity android:name=".ui.feature.todo.AddUpdateTodoActivity" />
        <activity android:name=".ui.feature.finance.FinanceActivity" />
        <activity android:name=".feature_note.presentation.note.HomeActivity" />
        <activity android:name=".ui.feature.intro.OnBoardingActivity" />
        <activity
            android:name=".core.MainActivity"
            android:noHistory="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>